import { describe, it, expect, beforeAll } from "vitest"

import { mount } from "@vue/test-utils";
import LoginView from "../LoginView.vue";

//describe(

// TODO: setValue on wrapper.elements to test valid and invalid inputs against a validation
// (too short/long enough password, contains characters...)